<div id='container-show-user'>
  <div class="user-top-background">
    <div class="user-buttons-top">
      <%= link_to organisation_path, class: 'btn return-arrow' do %><i class="fas fa-arrow-left"></i><% end %>
      <div id='show-buttons'>
        <% if current_user.access_level == 'Super Admin' || @user == current_user || (['Account Owner', 'HR'].include?(current_user.access_level) && current_user.company == @user.company) %>
          <a data-toggle='modal' data-target='#editUser'><button class='btn-edit-white btn-icon' data-target='tooltip' title='Edit User' style='margin-right: 5px; border-radius: 5px'><i class='fas fa-pen'></i></button></a>
          <a data-toggle='modal' data-target='#editPassword'><button class='btn-edit-white btn-icon btn-last' data-target='tooltip' title='Change Password' style='margin-right: 5px; border-radius: 5px'><i class="fas fa-key"></i></button></a>
        <% end %>
      </div>
    </div>
  </div>
  <div class="user-grid">
    <div class="user-top">
      <div class="user-photo">
        <% if @user.picture.present? %>
          <img src='<%= @user.picture %>' alt="" class='avatar-xlarge' onerror='this.onerror=null;this.src="<%= asset_url('empty-avatar.png', type: :image) %>";'>
        <% else %>
            <%=  image_tag 'empty-avatar.png', class:'avatar-xlarge' %>
        <% end %>
      <div class="user-main-info">
        <div class='user-name'>
          <p><strong><%=  @user.firstname %> <%=  @user.lastname %></strong></p>
        </div>
        <div class="user-information">
          <%= render 'users/user_informations', user: @user %>
        </div>
      </div>
      </div>
      <div class="user-top-info">
        <%= render 'user_contact_info', user: @user %>
      </div>
      <div class="user-basic-info">
        <%= render 'user_basic', user: @user %>
      </div>
    </div>
  </div>
    <p class='trainings-title'>Trainings</p>
  <div class="user-trainings">
    <div class="user-info-header">
      <p style='font-size: 12px'>Interested</p>
    </div>
    <div class="trainings-row">
      <%= render 'user_interests', user: @user %>
    </div>
    <div class="user-info-header">
      <p style='font-size: 12px'>Upcoming</p>
    </div>
    <div class="trainings-row">
      <% Session.joins(:attendees).where(attendees: {user_id: @user.id}).where('date >= ?', Date.today).each do |session| %>
        <% content = Session.includes([:content]).find(session.id).content %>
        <% if session.date == Date.today %>
        <%= link_to content_path(content, redirect_from: 'user_show', session_id: session.id) do %>
          <% if content.content_type == 'Synchronous' %>
            <div class="booking-date">
              <p style='margin-right:5px; color:#F26419;'>Begins</p>
              <b><p style='color:#F26419;'><%= session.date.strftime('%B %d') %></p></b>
            </div>
            <div class="booklet-card">
              <div class="booklet-card-syn">
                <div class="booklet-card-top">
                  <div class="booklet-card-top-info">
                  <h3><%= content.title %></h3>
                  <% interest = UserInterest.find_by(content_id: session.content_id, user_id: current_user.id) %>
                  <p><%= content.duration %> Minutes</p>
                  </div>
                  <div class="booklet-card-type-pill">
                    <div class="booklet-card-type">
                      <div class="booklet-card-type-icon">
                        <% if content.content_type == 'Synchronous' %>
                          <i class="fas fa-chalkboard-teacher" style='color:#F26419;'></i>
                          <p>In Person</p>
                        <% else %>
                          <i class="fas fa-laptop" style='color:black;'></i>
                          <p>Online</p>
                        <% end %>
                      </div>
                    </div>
                  </div>
                  <div class="booklet-top-right-icon-catalogue" >
                  </div>
                </div>
                <div class="booklet-card-bottom">
                  <div class="booked-date">
                    <% if session.date <= Date.today && (session.available_date.nil? || session.available_date >= Date.today) %>
                      <%= link_to '', content_path(content.id, content_access: 'granted', redirect_from: 'dashboard'), class: 'stretched-link' %>
                    <% else %>
                      <%= link_to '', content_path(content.id, session_id: session.id, redirect_from: 'dashboard'), class: 'stretched-link' %>
                    <% end %>
                  </div>
                  <div class="booklet-card-theme">
                  <% content.categories.order(title: :asc).each do |category| %>
                  <p><%= category.title %></p>
                  <% end %>
                  </div>
                  <div class="booklet-card-objective">
                  <% if content.description.present? %>
                    <% if content.description.to_plain_text.length >= (200) %>
                      <p><%= content.description.to_plain_text.first(200) + '...' %></p>
                    <% else %>
                      <p><%= content.description.to_plain_text.first(200) %></p>
                    <% end %>
                  <% end %>
                  </div>
                  <div class="booklet-card-like">
                    <p><%= session.attendees.count %> Participant(s)</p>
                  </div>
                </div>
              </div>
            </div>
            <% else %>
            <div class="booking-date">
              <p style='margin-right:5px;'>Available</p>
              <b><p><%= session.date.strftime('%B %d') %></p></b>
            </div>
            <div class="booklet-card">
              <div class="booklet-card-asyn">
                <div class="booklet-card-top">
                  <div class="booklet-card-top-info">
                  <h3><%= content.title %></h3>
                  <% interest = UserInterest.find_by(content_id: session.content_id, user_id: current_user.id) %>
                  <p><%= content.duration %> Minutes</p>
                  </div>
                  <div class="booklet-card-type-pill">
                    <div class="booklet-card-type">
                      <div class="booklet-card-type-icon">
                        <% if content.content_type == 'Synchronous' %>
                          <i class="fas fa-chalkboard-teacher" style='color:#F26419;'></i>
                          <p>In Person</p>
                        <% else %>
                          <i class="fas fa-laptop" style='color:black;'></i>
                          <p>Online</p>
                        <% end %>
                      </div>
                    </div>
                  </div>
                  <div class="booklet-top-right-icon-catalogue">
                  </div>
                </div>
                <div class="booklet-card-bottom">
                  <div class="booked-date">
                    <% if session.date <= Date.today && (session.available_date.nil? || session.available_date >= Date.today) %>
                      <%= link_to '', content_path(content.id, content_access: 'granted', redirect_from: 'dashboard'), class: 'stretched-link' %>
                    <% else %>
                      <%= link_to '', content_path(content.id, session_id: session.id, redirect_from: 'dashboard'), class: 'stretched-link' %>
                    <% end %>
                  </div>
                  <div class="booklet-card-theme">
                  <% content.categories.order(title: :asc).each do |category| %>
                  <p><%= category.title %></p>
                  <% end %>
                  </div>
                  <div class="booklet-card-objective">
                  <% if content.description.present? %>
                    <% if content.description.to_plain_text.length >= (200) %>
                      <p><%= content.description.to_plain_text.first(200) + '...' %></p>
                    <% else %>
                      <p><%= content.description.to_plain_text.first(200) %></p>
                    <% end %>
                  <% end %>
                  </div>
                  <div class="booklet-card-like">
                    <p><%= session.attendees.count %> Participant(s)</p>
                  </div>
                </div>
              </div>
            </div>
            </div>
          <% end %>
        <% end %>
      <% end %>
      <% end %>
    </div>
    <div class="user-info-header">
      <p style='font-size: 12px'>Completed</p>
    </div>
      <div class="trainings-row">
        <% Session.joins(:attendees).where(attendees: {user_id: @user.id}).where('date < ?', Date.today).each do |session| %>
          <% content = Session.includes([:content]).find(session.id).content %>
          <%= link_to content_path(content, redirect_from: 'user_show', content_access: 'granted') do %>
            <% if content.content_type == 'Synchronous' %>
              <div class="booklet-card">
                <div class="booklet-card-syn">
                  <div class="booklet-card-top">
                    <div class="booklet-card-top-info">
                    <h3><%= content.title %></h3>
                    <% interest = UserInterest.find_by(content_id: session.content_id, user_id: current_user.id) %>
                    <p><%= content.duration %> Minutes</p>
                    </div>
                    <div class="booklet-card-type-pill">
                      <div class="booklet-card-type">
                        <div class="booklet-card-type-icon">
                          <% if content.content_type == 'Synchronous' %>
                            <i class="fas fa-chalkboard-teacher" style='color:#F26419;'></i>
                            <p>In Person</p>
                          <% else %>
                            <i class="fas fa-laptop" style='color:black;'></i>
                            <p>Online</p>
                          <% end %>
                        </div>
                      </div>
                    </div>
                    <div class="booklet-top-right-icon-catalogue" >
                    </div>
                  </div>
                  <div class="booklet-card-bottom">
                    <div class="booked-date">
                      <%= session.date.strftime('%d/%m/%Y') %>
                    </div>
                    <div class="booklet-card-theme">
                    <% content.categories.order(title: :asc).each do |category| %>
                    <p><%= category.title %></p>
                    <% end %>
                    </div>
                    <div class="booklet-card-objective">
                    <% if content.description.present? %>
                      <% if content.description.to_plain_text.length >= (200) %>
                        <p><%= content.description.to_plain_text.first(200) + '...' %></p>
                      <% else %>
                        <p><%= content.description.to_plain_text.first(200) %></p>
                      <% end %>
                    <% end %>
                    </div>
                    <div class="booklet-card-like">
                      <div class='content-time'><p><i class="fas fa-star-half-alt" style='color:#F26419;'></i>
                        <% assessment = session.content.mods.where(mod_type: 'assessment').first %>
                        <% if assessment.present? %>
                          <% Assessment.joins(:user_forms).where(user_forms: {user_id: @user.id, mod_id: assessment.id}).each do |form| %>
                           Grade : <%= UserForm.find_by(user_id: @user, mod_id: form.id)&.grade %>%
                          <% end %></p>
                        <% else %>
                          <p>No Quiz</p>
                        <% end %>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
              <% else %>
               <div class="booklet-card">
                <div class="booklet-card-syn">
                  <div class="booklet-card-top">
                    <div class="booklet-card-top-info">
                    <h3><%= content.title %></h3>
                    <% interest = UserInterest.find_by(content_id: session.content_id, user_id: current_user.id) %>
                    <p><%= content.duration %> Minutes</p>
                    </div>
                    <div class="booklet-card-type-pill">
                      <div class="booklet-card-type">
                        <div class="booklet-card-type-icon">
                          <% if content.content_type == 'Synchronous' %>
                            <i class="fas fa-chalkboard-teacher" style='color:#F26419;'></i>
                            <p>In Person</p>
                          <% else %>
                            <i class="fas fa-laptop" style='color:black;'></i>
                            <p>Online</p>
                          <% end %>
                        </div>
                      </div>
                    </div>
                    <div class="booklet-top-right-icon-catalogue" >
                    </div>
                  </div>
                  <div class="booklet-card-bottom">
                    <div class="booked-date">
                      <%= session.date.strftime('%d/%m/%Y') %>
                    </div>
                    <div class="booklet-card-theme">
                    <% content.categories.order(title: :asc).each do |category| %>
                    <p><%= category.title %></p>
                    <% end %>
                    </div>
                    <div class="booklet-card-objective">
                    <% if content.description.present? %>
                      <% if content.description.to_plain_text.length >= (200) %>
                        <p><%= content.description.to_plain_text.first(200) + '...' %></p>
                      <% else %>
                        <p><%= content.description.to_plain_text.first(200) %></p>
                      <% end %>
                    <% end %>
                    </div>
                    <div class="booklet-card-like">
                      <div class='content-time'><p><i class="fas fa-star-half-alt"></i>
                        <% assessment = session.content.mods.where(mod_type: 'assessment').first %>
                        <% if assessment.present? %>
                          <% Assessment.joins(:user_forms).where(user_forms: {user_id: @user.id, mod_id: assessment.id}).each do |form| %>
                           Grade : <%= UserForm.find_by(user_id: @user, mod_id: form.id)&.grade %>%
                          <% end %></p>
                        <% else %>
                          <p>No Quiz</p>
                        <% end %>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            <% end %>
          <% end %>
        </div>
        <% end %>
    </div>
  </div>
</div>


<!-- Modal -->
<div class="modal fade" id="editUser" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
  <div class="modal-dialog" role="document" style="border-radius: 20px">
    <%= render 'edit', user: @user %>
  </div>
</div>

<div class="modal fade" id="editPicture" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
  <div class="modal-dialog" role="document" style="border-radius: 20px">
    <%= render 'edit_picture', user: @user %>
  </div>
</div>

<div class="modal fade" id="editPassword" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
  <div class="modal-dialog" role="document" style="border-radius: 20px">
    <%= render 'changepassword', user: @user %>
  </div>
</div>


<% if ['Super Admin', 'Account Owner', 'HR'].include?(current_user.access_level) %>
  <script>
    function openForm(element) {
      if (document.querySelector('.opened') == null) {
        form = element.parentNode.querySelector('.hidden-form-user');
        element.classList.toggle('hidden');
        form.classList.toggle('hidden');
        form.classList.toggle('opened');
      }
    };

    function fireDatePicker() {
      flatpickr(".datepicker", {
        disableMobile: true,
        dateFormat: "d/m/Y",
      })
    };
  </script>
  <script>
  sliders = document.querySelectorAll('.trainings-row');
  sliders.forEach((slider) => {
    isDown = false;
    let startX;
    let scrollLeft;

    slider.addEventListener('mousedown', (e) => {
      isDown = true;
      slider.classList.add('active');
      startX = e.pageX - slider.offsetLeft;
      scrollLeft = slider.scrollLeft;
    });
    slider.addEventListener('mouseleave', () => {
      isDown = false;
      slider.classList.remove('active');
    });
    slider.addEventListener('mouseup', () => {
      isDown = false;
      slider.classList.remove('active');
    });
    slider.addEventListener('mousemove', (e) => {
      if(!isDown) return;
      e.preventDefault();
      const x = e.pageX - slider.offsetLeft;
      const walk = (x - startX) * 3; //scroll-fast
      slider.scrollLeft = scrollLeft - walk;
      console.log(walk);
    });
  })

  // window.onload = function navBarPadding() {
  // navbar = document.querySelector('.top-left-nav')
  // navbar.style.paddingBottom = '4px';
  // };
</script>
<% end %>
