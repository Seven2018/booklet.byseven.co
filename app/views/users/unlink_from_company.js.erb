array = "<%= @selected_users %>".split(',')
array.forEach((user_id) => {
  document.querySelectorAll('.user-card-' + user_id).forEach((card) => {
    card.remove()
  })
})
main_container.querySelector('.organisation__dropdown-menu').classList.remove('show')
document.querySelector('.modal.show').querySelector('.cancel-close').click()
