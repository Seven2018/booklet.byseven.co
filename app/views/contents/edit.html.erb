<div class="container-new-form">
  <div class="training-info-headers">
    <%= link_to url_for(:back), class: 'btn return-arrow' do %>
      <i class="fas fa-arrow-left"></i>
    <% end %>
  </div>
  <div class="interactive-form">
    <div class="form-wrapper">
      <h2>Edit: <%= @content.title %></h2>

      <%= simple_form_for @content do |f| %>
        <%= f.input :title %>
        <%= f.input :duration, label: 'Duration (min)' %>
        <%# f.input :image, label: 'Image URL' %>
    <!--     <ffn class="_ffn_textNode" data-ffn-css-id="5" style="border-bottom-color: rgba(0, 0, 0, 0);">Categories</ffn>
        <div id='checkbox' class='flex-column-centered'>
          <%# collection_check_boxes(:content, :category_ids, Category.all, :id, :title) do |b| %>
            <%# b.label class: 'label-checkbox', style: 'color:black;' do %>
              <%# b.check_box + b.text %>
            <%# end %>
          <%# end %>
        </div> -->
        <%= f.label :description %>
        <%= f.cktext_area :description %>
        <div style="display:flex;justify-content:center;margin-top:30px;">
          <%= button_tag :submit, class: 'btn btn-edit-green' do %>
            <p>SAVE <i class="fas fa-save"></i></p>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</div>


<script>
  $('#checkbox input:checkbox:checked').parent().toggleClass('checked-box');
  $('#checkbox input:checkbox').change(function () {
    $(this).parent().toggleClass('checked-box');
  });
</script>


