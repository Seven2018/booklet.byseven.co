<% content_for :title do %>
  Check your mailbox !
<% end %>
<% content_for :subtitle do %>
  <div class="flew-row mb-4 width-70">
    We just sent an email to the adress
    <strong class="underline"><%= @user.email %></strong> to let you
    create a beautiful password.
  </div>
  <div class="flew-row mb-4 width-70">
    If you don’t receive it, click here so we can
    send it one more time (please check your
    spams first 🙏).
  </div>
<% end %>

<div class="">
  <%= link_to u_resend_email_path(email: @user.email) do %>
    <button class="width-70 bkt-bg-dark-grey bkt-white rounded-2px mt-4 p-3">Send it again</button>
  <% end %>
</div>

<% flash.each do |type, msg| %>
  <% if type == 'alert' %>
    <div class="bkt-red mt-2">
      <%= msg %>
    </div>
  <% else %>
    <div class="bkt-green mt-2">
      <%= msg %>
    </div>
  <% end %>
<% end %>