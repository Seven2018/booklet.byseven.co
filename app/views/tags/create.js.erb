document.querySelector('.organisation-manage-users-dropright').innerHTML = '<%= j render("pages/organisation_manage_users_dropright", tag_categories: @tag_categories) %>';
opened = '<%= @opened %>'.split(',')
console.log(opened);
document.querySelectorAll('.organisation-manage-users-block').forEach((block) => {
  console.log(opened.includes(block.getAttribute('data-category')));
  if (opened.includes(block.getAttribute('data-category'))) {
    block.style.maxHeight = '1000px';
    block.classList.add('active');
  }
})
