submit_button = document.querySelector('#submit-interview')
if (<%= @fully_answered %> && submit_button != null) {
  submit_button.classList.remove('btn-blue-disabled')
  submit_button.disabled = false
} else if (submit_button != null) {
  submit_button.classList.add('btn-blue-disabled')
  submit_button.disabled = true
}
