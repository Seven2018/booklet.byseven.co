<div class="container-new-form">
  <div class="training-info-headers">
    <%= link_to url_for(:back), class: 'btn return-arrow' do %>
      <i class="fas fa-arrow-left"></i>
    <% end %>
  </div>
   <div class="interactive-form">
    <div class="form-wrapper">
      <h2>Create a new module</h2>
      <%= simple_form_for Mod.new do |f| %>
        <%= f.input :title %>
        <%= f.input :duration %>
        <%= f.input :document, label: 'Document (url)' %>
        <%= f.input :media, label: 'Media (url)' %>
        <%= f.label :content %>
        <%= f.cktext_area :content %>
        <div style=" display: flex; justify-content:center;margin-top:3rem;">
          <% if @workshop.present? %>
            <%= button_tag type: 'submit', name: 'workshop_id', value: "#{@workshop.id}", class: 'btn btn-edit-green' do %>
              <p><b>SAVE</b> <i class="fas fa-save"></i></p>
            <% end %>
          <% elsif @training_workshop.present? %>
            <%= button_tag type: 'submit', name: 'training_workshop_id', value: "#{@training_workshop.id}", class: 'btn btn-edit-green' do %>
             <p><b>SAVE</b><i class="fas fa-save"></i></p>
            <% end %>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</div>
