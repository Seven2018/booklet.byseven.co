<div class="modal-content">
  <div class="modal-header">
    <div id="modal-title" style=" display: flex; justify-content: space-between;">
      <h3 class="modal-title" id="myModalLabel">New module</h3>
    </div>
    <button type="button" class="btn btn-icon" data-dismiss="modal" aria-label="Close"><i class="fas fa-times"></i></button>
  </div>
  <div class="modal-body" id="modalNewBookinBody">
    <%= simple_form_for Mod.new(content: nil) do |f| %>
      <%= f.input :title %>
      <%= f.input :duration %>
      <%= f.rich_text_area :content %>
      <div style=" display: flex; justify-content: space-between;margin-top:3rem;">
        <% if @content.present? %>
          <%= button_tag type: 'submit', name: 'content_id', value: "#{@content.id}", class: 'btn btn-edit-green' do %>
            <p><b>SAVE</b> <i class="fas fa-save"></i></p>
          <% end %>
        <% elsif @training_content.present? %>
          <%= button_tag type: 'submit', name: 'training_content_id', value: "#{@training_content.id}", class: 'btn btn-edit-green' do %>
            <p><b>SAVE</b> <i class="fas fa-save"></i></p>
          <% end %>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
