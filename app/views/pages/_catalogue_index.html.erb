<div class="row">
  <% contents.each do |content| %>
    <div class="booklet-col col-xl-4 col-lg-6 col-md-12" id="content-<%= content.id %>">
      <%= render 'pages/catalogue_content_card', content: content, contents: contents %>
    </div>
  <% end %>
</div>
