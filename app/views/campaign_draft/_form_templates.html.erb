<%= render_component FormSectionTitleComponent.new(title: t('campaign_draft.edit.choose_how_display_template')) %>

<div class="d-flex gap-1rem w-100" style='max-width: 500px;'>

  <%= render_component RadioButtonTileComponent.new(
    model: :templates_selection_method,
    name: :single,
    checked: campaign.templates_selection_method == :single.to_s || campaign.templates_selection_method.nil?,
    title: "Single template",
    subtitle: t('campaign_draft.edit.select_participant_from_list')
  ) do %>
    <%# TODOCHANGEICON %>
    <span class="iconify fs-3rem" data-icon="akar-icons:file"></span>
  <% end %>

  <%= render_component RadioButtonTileComponent.new(
    model: :templates_selection_method,
    name: :multiple,
    checked: campaign.templates_selection_method == :multiple.to_s,
    title: "Multiple templates",
    subtitle: t('campaign_draft.edit.add_participant_by_arrival'),
    disabled: true
  ) do %>
    <span class="iconify fs-3rem" data-icon="akar-icons:network"></span>
  <% end %>

</div>

<%# TODO %>
<%= select_tag :default_template_id, options_for_select(InterviewForm.unused.where(company_id: current_user.company_id).pluck(:title, :id), selected: @campaign.default_template_id), class: 'border-bkt-dark-grey rounded-5px p-0_75rem' %>
